Homework for Chapter 2 of Learning MySQL (2006 edition), from p. 94:

Exercises:
1) What is command completion? Is a time saving feature that finishes the name of a file, directory or commnd when you hit the tab key.

2) What are the relative advantages of installing MySQL using the package,
 directory archive (tarball or "no-install", or compiled methods?  Installing allows for less error and is quicker whereas compling allows for more optimiation.

3) How do you verify the integrity of downloaded packages?
Compare the checksums generated by a digest algorithm such as MD5.

4) How do you add the MySQL bin directory to the operating system path?
Installing the Windows version, I selected "Include Bin Directory in Windows Path" on the Configure Windows screen.

Additional Exercises (not in the book): 
1) Document how you installed MySQL and any issues you came across with installation. 
 (The goal is to be easy to install it again on a similar machine.)
I downloaded the Windows version from MySql AB Websit.
I started the installer program so it would install in C:\Program Files\MySQL\MySQL Server 5.0\directory.
I follow the prompts from the MySQL  Server Instance Configuartion Wizard.
I select "include bin directory in Windows Path".
I select a new root user password.
The installation process completes.

2) Document where important files should be.
Important files should be in the bin directory.

Do not forget to do a pull request when your homework is complete.
